name: amazon runner
on:
  push:
    branches: [main]
  pull_requests:
    branches: [main]
    
jobs:
  build:
      runs_on: ubuntu-latest
      steps:
        - name: start build
          run: echo "hello cloudgurus"
      
  test:
    runs_on: ubuntu-latest
    needs: build
    steps:
      - name: start test
        run: echo "testing phase"
  
  deploy:
    runs_on: ubuntu-latest
    needs: test
    steps:
      - name: start deploy
        run: echo "deployment phase"
      
    

  
